import { HighlightMessage } from './highlight-message';

describe('HighlightMessage', () => {
  it('should create an instance', () => {
    const directive = new HighlightMessage();
    expect(directive).toBeTruthy();
  });
});
