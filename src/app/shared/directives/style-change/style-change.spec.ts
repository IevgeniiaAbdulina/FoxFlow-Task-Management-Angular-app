import { StyleChange } from './style-change';

describe('StyleChange', () => {
  it('should create an instance', () => {
    const directive = new StyleChange();
    expect(directive).toBeTruthy();
  });
});
